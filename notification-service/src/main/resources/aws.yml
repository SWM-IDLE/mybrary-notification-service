cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}

    region:
      static: ${AWS_REGION}

    stack:
      auto: false

    sns:
      topic:
        arn: ${AWS_SNS_TOPIC_ARN}

    sqs:
      queue:
        name: ${AWS_SQS_QUEUE_NAME}